panelWidth = panel.getWidth();
averagePos = getAveragePos(dogArray);
if (average x_pos > (0.5 * panel_width) {
	viewPos = 0;
	} else if (averageX < (bgImage_width - (panel_width * 0.5))) {
	viewPos = bgImage_width - panelWidth
	} else {
	// panel x-coordinate of background
	view_pos = average of x_pos's - (0.5 * panel_width)  (--> absolute x position of left edge)
	}
// draw dog at x_pos - view_pos

private int getAveragePos(Dog[] dogArray) {
	double sum;
	for (dog d: dogArray) {
		sum += d.get_x();
	}
	return = (int) Math.floor(sum / dogArray.length());
}